/** @format */

import { Button } from "@/components/ui/button";

export function Button({text}) {
	
	return (
		<Button className='colors' style={{ backgroundColor: props.color }}>
			Button
		</Button>
	);
}
